# Handwritten-Digit-Recognizer
Built a deep learning model using Convolutional Neural Networks to recognize handwritten digits from the MNIST dataset with high accuracy. Integrated the model into a GUI application using Tkinter, enabling users to draw digits and get real-time predictions. The system processes and classifies user input with live confidence scores.
